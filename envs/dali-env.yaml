{% if build_type == 'cuda' %}
imported_envs:
  - tensorflow-env.yaml

packages:
  - feedstock : opencv
  - feedstock : nvcc
  - feedstock : DALI 
  - feedstock : jpeg-turbo
  - feedstock : libflac
  - feedstock : libsndfile
{% endif %}
