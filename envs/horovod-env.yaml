imported_envs:
{% if build_type == 'cuda' %}
  - pytorch-env.yaml
  - tensorflow-env.yaml

channels:
  - https://public.dhe.ibm.com/ibmdl/export/pub/software/server/ibm-ai/conda/

packages:
  - feedstock : horovod
  - feedstock : openmpi
{% endif %}
