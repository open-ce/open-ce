From e4fedf9ceb298c020603fcb18c6de3e1cc084d3e Mon Sep 17 00:00:00 2001
From: Aman Surkar <Aman.Surkar@ibm.com>
Date: Mon, 1 Jul 2024 13:34:40 +0000
Subject: [PATCH] Opence changes for tiktoken

---
 recipe/meta.yaml | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/recipe/meta.yaml b/recipe/meta.yaml
index b6c7378..63c243b 100644
--- a/recipe/meta.yaml
+++ b/recipe/meta.yaml
@@ -12,26 +12,26 @@ source:
 build:
   script:
     - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
-    - {{ PYTHON }} -m pip install . -vv
+    - {{ PYTHON }} -m pip install . --no-deps -vv
   number: 0
 
 requirements:
   build:
-    - python                                 # [build_platform != target_platform]
+    - python {{python}}                                 # [build_platform != target_platform]
     - cross-python_{{ target_platform }}     # [build_platform != target_platform]
     - {{ compiler('cxx') }}
     - {{ compiler('rust') }}
     - cargo-bundle-licenses
   host:
-    - python
-    - setuptools >=62.4
+    - python {{python}}
+    - setuptools {{setuptools}}
     - wheel
     - setuptools-rust >=1.5.2
-    - pip
+    - pip {{pip}}
   run:
-    - python
+    - python {{python}}
     - regex >=2022.1.18
-    - requests >=2.26.0
+    - requests {{requests}}
 
 test:
   imports:
-- 
2.40.1

