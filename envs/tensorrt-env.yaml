packages:
{% set py_ver = python | float %}
{% if build_type == 'cuda' and py_ver < 3.8 %}
  - feedstock : cudnn
  - feedstock : tensorrt
  - feedstock : onnx
  - feedstock : protobuf          #[python == '3.8']
{% endif %}
