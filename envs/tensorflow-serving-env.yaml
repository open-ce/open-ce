imported_envs:
  - bazel-env.yaml
{% if build_type == 'cuda' %}
  - tensorrt-env.yaml
{% endif %}

packages:
{% if build_type == 'cuda' %}
  - feedstock: nccl
  - feedstock: cudnn
{% endif %}
  - feedstock: libevent
  - feedstock: tensorflow-serving
  - feedstock: protobuf   #[ppc64le and python == '3.8']
