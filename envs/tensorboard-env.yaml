builder_version: ">=10.0.1"

imported_envs:
  - bazel-env.yaml

packages:
{% if not s390x %}
  - feedstock : tensorboard-data-server
  - feedstock : tensorboard
{% endif %}

git_tag_for_env: open-ce-v1.6.1
