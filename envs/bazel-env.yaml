builder_version: ">=10.0.1"

packages:
  - feedstock : cdts               #[not x86_64]
    runtime_package : False        #[not x86_64]
  - feedstock : bazel
    runtime_package : False

git_tag_for_env: open-ce-v1.7.0
