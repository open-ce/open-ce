imported_envs:
  - tensorboard-env.yaml
  - pytorch-env.yaml

packages:
{% if not s390x %}
  - feedstock : pytorch-lightning
  - feedstock : pytorch-lightning-bolts
  - feedstock : libflac
  - feedstock : libsndfile
{% endif %}

git_tag_for_env: open-ce-v1.5.6
