imported_envs:
{% if build_type == 'cuda' %}
  - dask-cuda-env.yaml
{% endif %}

packages:
{% if build_type == 'cuda' %}
  - feedstock : cudatoolkit    #[cudatoolkit == "11.2"]
  - feedstock : nccl
{% endif %}
  - feedstock : xgboost
